{
  "buildCommand": "cd ../.. && pnpm turbo run build --filter=@near/near-com",
  "outputDirectory": "apps/near-com/.next",
  "installCommand": "pnpm install --frozen-lockfile",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/cron/publish-scheduled",
      "schedule": "* * * * *"
    }
  ]
}
